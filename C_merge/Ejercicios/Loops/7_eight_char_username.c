#include <stdio.h>

int valid_username(char[20]);

int main(){
    char usuario[20];
    
    printf("Digite un nombre de usuario válido \n");
    scanf("%s", usuario);

    valid_username(usuario);
}

int valid_username(char[20]){
    
}